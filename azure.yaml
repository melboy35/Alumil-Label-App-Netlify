# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: alumil-inventory-system
metadata:
  template: alumil-inventory-system@0.0.1-beta
services:
  web:
    project: ./web
    language: js
    host: staticwebapp